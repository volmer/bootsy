<div class="page-header">
  <h1><%= @post.title %></h1>
</div>

<div class="clearfix">
  <%= raw @post.content %>
</div>

<hr />

<%= link_to 'Edit', edit_post_path(@post), :class => 'btn' %>

<hr />

<%= render 'comments/index', {post: @post} %>
