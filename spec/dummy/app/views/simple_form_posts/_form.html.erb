<%= simple_form_for(@post, url: simple_form_posts_path) do |f| %>
  <%= f.error_notification %>

  <%= f.input :title %>
  <%= f.input :content, as: :bootsy, input_html: { rows: 16 } %>

  <%= f.button :submit %>
<% end %>
